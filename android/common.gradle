// Define versions in a single place to ensure compatibility across all Android modules
ext {
    // SDK and tools
    minSdkVersion = 21
    targetSdkVersion = 33
    compileSdkVersion = 33
    
    // App dependencies
    supportLibVersion = "28.0.0"
    kotlinVersion = "1.7.0"
    
    // React Native specific versions
    buildToolsVersion = "33.0.0"
    ndkVersion = "22.1.7171670"
}
